#include "MainPlayButton.h"

MainPlayButton::MainPlayButton(int x, int y, MainMenu* menu, SDL_Texture* activeTexture, SDL_Texture* inactiveTexture) : MainButton(x, y, menu, activeTexture, inactiveTexture) {
	
}

void MainPlayButton::handleInput(InputData data) {

}

